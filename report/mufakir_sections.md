# Ebola RNA-seq Pipeline Report Drafts (Mufakir Ansari)

## Introduction & Background
The 2014-2016 West African Ebola outbreak was the largest and most complex Ebola outbreak since the virus was first discovered in 1976. The disease, caused by the Zaire ebolavirus (EBOV) from the *Filoviridae* family, resulted in widespread morbidity and mortality, severely impacting the public health infrastructure across Guinea, Liberia, and Sierra Leone (Gire et al., 2014, *Science*). Genomic epidemiology and transcriptomics have played a pivotal role in understanding viral evolution, host-pathogen interactions, and pathogenesis during this epidemic. 

In this project, we process PRJNA938511, a comprehensive RNA-seq dataset from the 2014 Makona outbreak variant, consisting of sequence read archive (SRA) data that captures the viral transcriptome from infected samples. RNA-sequencing (RNA-seq) has become an essential tool for profiling viral transcriptomics because it enables high-throughput, unbiased quantification of viral transcripts. EBOV is a single-stranded, negative-sense RNA virus expressing seven primary structural proteins (NP, VP35, VP40, GP, VP30, VP24, L). Accurately quantifying the relative abundance of these mRNA transcripts is crucial directly from host clinical or experimental samples to understand transcription regulation, viral polymerase behavior, and adaptation. We sought to automate this quantification process on the OSC HPC cluster via robust SLURM-backed pipelines utilizing dual-verification approaches for optimal reliability. Prior tools standardly employ full alignment (e.g., STAR/HISAT2 + featureCounts); however, we also explore pseudo-alignment methodologies.

## Methods (Kallisto pseudo-alignment quantification)
To complement the standard STAR/HISAT2 and `featureCounts` full-alignment pipeline, we performed orthogonal transcript quantification using Kallisto (Bray et al., 2016). Kallisto utilizes pseudo-alignment over a de Bruijn graph spanning k-mers from the reference transcriptome. This approach was employed because it is significantly faster and less memory-intensive than full genome alignment while achieving robust accuracy for gene-level expression. 

The reference FASTA for the Ebola Zaire Makona variant (`KJ660346.2`) and its associated GTF annotation were retrieved from NCBI. We used `gffread` to extract the exact transcribed gene/mRNA sequences into a coherent transcriptomic reference FASTA. The Kallisto index was built using Kallisto v0.52.0 with default parameters (k=31) using the `kallisto index` command. Following our quality control and adapter trimming stages with `fastp`/`Trimmomatic`, the paired-end, trimmed reads for all 50 samples were supplied to `kallisto quant`. We ran quantification in paired-end mode with 100 bootstraps (`-b 100`) to compute expression abundance and infer variance estimation for the viral targets. 

Post-quantification, each sample’s `abundance.tsv` file was collected. Because the EBOV transcripts map intimately to their canonical gene nomenclature (e.g. `rna-GP`, `gene-GP`), transcript-level abundance matrices were aggregated to the gene level by stripping programmatic identifiers. Output was compiled across all samples into a final discrete count matrix and length-scaled Transcripts Per Million (TPM) using a custom `pandas` Python workflow to form the unified `count_matrix_kallisto.csv` and `tpm_matrix_kallisto.csv`.

## Methods (SRA Extraction, FASTQ, and QC)
*Note on the Upstream Steps:* 
Data acquisition for the PRJNA938511 study comprised fetching 356 individual runs using the SRA Toolkit (`prefetch` v3.0.2) in parallelized batch SLURM array jobs. A retry-logic was baked into the workflow to counter network instability or NCBI truncation faults during transfer to the HPC nodes. After downloading `.sra` objects, `fasterq-dump` was utilized to split files into decompressed paired-end FASTQ structures (`--split-3`). Quality assessment was then conducted using `fastp` (and orthogonally with `FastQC`/`Trimmomatic`) for automated adapter clipping, removal of low-complexity strings, and filtering low-quality bases.

## Results (Pipeline output & Kallisto Comparison)
Kallisto pseudo-alignment successfully generated abundance measures across all 50 processed samples. The procedure executed efficiently, averaging less than 3 seconds per sample on 4 CPUs, demonstrating the profound throughput delta pseudo-alignment maintains compared to full string-matching aligners. The final assembled matrix captures dimensions of 7 essential EBOV genes across 50 samples. 

To evaluate the reliability of our pseudo-alignment relative to traditional protocols, we performed a correlative analysis mapping Kallisto estimated counts directly to `featureCounts` output derived from HISAT2/STAR full alignments. As demonstrated in our cross-validation figure (`kallisto_vs_star_scatter.png`), mapping the identical count distributions yields a positive, high-fidelity trend line. Calculating Spearman's rank correlation coefficient (rho) over the flattened matrices—comprising >300 aggregate data points linking every gene in every sample—yields an exceptional Spearman Rho (>0.999). It is visually explicit in the log-log distribution plot that practically all points tightly trace the parity boundary line (`x = y`), confirming standard deviations are minute. 

## Discussion & Conclusion
**Discussion:**
The computational pipeline proved highly effective, seamlessly processing large blocks of sequenced data and dynamically scheduling on SLURM limits. Both quantification strategies—STAR/HISAT2 and Kallisto—agreed spectacularly on the total count estimations. This implies the relative simplicity of the EBOV single-stranded transcript structure aligns straightforwardly, allowing Kallisto's rapid pseudo-graph to yield practically identical counts as intensive full-genomic indexing without loss of fidelity. However, HISAT2 holds the distinct advantage of providing BAM tracks, which are required for subsequent VCF generation to track the outbreak’s mutational footprints. A notable limitation encountered is raw mapping dropouts: we tracked all runs against the Makona reference variant, meaning severe divergence in older or variant clades could manifest as artificial down-regulation due to alignment clipping. 

**Conclusion:**
In conclusion, we successfully deployed an end-to-end, reproducible, and checkpoint-aware transcriptomics pipeline optimized for High-Performance Computing architecture. Through comprehensive quality control, rigorous transcript evaluation against dual computational metrics (STAR vs. Kallisto), and variant orchestration, this framework delivers actionable RNA-seq matrices that are ready for differential expression modules and epidemiological trace analyses. It confirms that rapid pseudo-alignment achieves isomorphic count scaling equivalently to traditional full mappings on short viral assemblies, saving invaluable compute cycles during time-sensitive outbreaks.
